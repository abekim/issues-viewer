var app = app || {} //global app

$(function ($) {
  'use strict';

  app.AppView = Backbone.View.extend({

    el: '#container',

    //events to look out for
    events: {},

    initialize: function () {
      //initialize
    },

    render: function () {
      //render stuff here.
    }
  });
});
