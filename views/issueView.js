var app = app || {};

$(function () {
  'use strict';

  //viewing issues
  app.IssueView = Backbone.View.extend({

    //specify some tag here

    //load template somewhere around here

    initialize: function () {
      //events to listen for go here.
    },

    render: function () {
      //render render
    }
  });
});
